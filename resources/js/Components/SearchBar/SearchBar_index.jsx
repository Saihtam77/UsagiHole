import React from "react";
import SearchBar from './SearchBar';


const SearchBar_index = () => {
   return (
      <section>
        <SearchBar />
        <hr className="mx-5 border-black" />
      </section>
   );
};

export default SearchBar_index;
